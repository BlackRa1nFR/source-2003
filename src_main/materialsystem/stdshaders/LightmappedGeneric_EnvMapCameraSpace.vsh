vs.1.1

#include "LightmappedGeneric_inc.vsh"

$detail = 0;
$envmap = 1;
$envmapcameraspace = 1;
$envmapsphere = 0;
$vertexcolor = 0;

&LightmappedGeneric( $detail, $envmap, $envmapcameraspace, $envmapsphere, 
				$vertexcolor );

